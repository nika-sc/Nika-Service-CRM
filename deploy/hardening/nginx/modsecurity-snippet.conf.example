# Merge into host nginx server { } block (443) — one-time manual merge.
# modsecurity on;
# modsecurity_rules_file /etc/nginx/modsecurity/modsecurity.conf;

# Optional path exclusions after audit (example — adjust per modsec_audit.log):
# location /socket.io/ {
#     modsecurity off;
#     proxy_pass http://127.0.0.1:8080;
#     ...
# }
